#include <iostream>
#include <cmath>
using namespace std;
int main() {
	double x = 1.7;
	double r = pow((x + 1), 2) + 3 * (x + 1);
	cout << r << endl;
	int x1 = 3;
	int r1 = pow((x1 + 1), 2) + 3 * (x1 + 1);
	cout << r1 << endl;
	return 0;
}